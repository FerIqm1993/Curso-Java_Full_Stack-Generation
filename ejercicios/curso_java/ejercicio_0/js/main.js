import { inicializarFetchJava } from "./fetch.js";

// Inicializamos la escucha de los botones que consumen el backend de Java.
inicializarFetchJava();

console.log("🟢 Frontend listo y esperando a que corras el archivo Backend.java");
