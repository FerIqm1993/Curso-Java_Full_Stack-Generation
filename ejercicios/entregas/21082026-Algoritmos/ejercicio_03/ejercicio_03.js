// Calcular la raíz cuadrada de un número (Math.sqrt)
function calcularRaizCuadrada(numero) {
    let resultado = Math.sqrt(numero);
    console.log(resultado);
    return resultado;
}

console.log("Raíz cuadrada de 25:", calcularRaizCuadrada(25));
