// Elevar un número a una potencia n (Math.pow)
function elevarPotencia(base, exponente) {
    let resultado = Math.pow(base, exponente);
    return resultado;
}

console.log("2 elevado a la 3:", elevarPotencia(2, 3));

